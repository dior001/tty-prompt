{
  "blocked": false,
  "blocked_reason": null,
  "dependency_changes": [
    "tty-reader: ~> 0.8 -> ~> 0.9",
    "rake: unconstrained -> ~> 13.0",
    "rspec: >= 3.0 -> ~> 3.13",
    "benchmark-ips: ~> 2.13.0 -> ~> 2.15",
    "simplecov: ~> 0.22.0 -> ~> 1.0",
    "coveralls_reborn: ~> 0.28.0 -> ~> 1.0",
    "added rubocop ~> 1.88 (dev)",
    "added bundler-audit ~> 0.9 (dev)",
    "pastel: ~> 0.8 unchanged, already at latest 0.8.0",
    "removed dead RUBY_VERSION conditionals from Gemfile (Ruby 2.0/2.7 guards no longer reachable at the new floor)"
  ],
  "security_findings": [
    "bundler-audit check --update against ruby-advisory-db (1229 advisories): no vulnerabilities found for dependency set before or after upgrades"
  ],
  "coverage_before": 97.51,
  "coverage_after": 100.0,
  "review_fixes": [
    "appveyor.yml: pinned image: Visual Studio 2022 so the Ruby 3.2/3.3/3.4 matrix entries actually resolve on AppVeyor's Windows build workers (the default account image predates those Ruby installs, which was failing continuous-integration/appveyor/pr); no Ruby version was added, dropped, or narrowed",
    "examples/ask.rb:10: Layout/LineLength (87/80) - reformatted the prompt.ask(...) keyword arguments one per line; examples/**/* is excluded from the project's own rubocop run but a reviewer's tooling checked the file directly, which is standard RuboCop behavior for an explicitly-targeted path",
    "spec/unit/multi_select_spec.rb:347: Layout/LineLength (87/80) - wrapped the hint: string concatenation across an extra line so every fragment is <=80 columns, concatenated value unchanged; spec/**/* is deliberately excluded from this cop in .rubocop.yml (pre-existing, not introduced by this PR) but the reviewer flagged the line directly",
    "spec/unit/question/validation/call_spec.rb:36: Style/LambdaCall - replaced all four lambda.() call sites in the file with lambda.call(...); .() shorthand is ordinary syntax well within the gem's >= 3.2.0 floor, so this was a pure style fix with no compatibility implications"
  ]
}
